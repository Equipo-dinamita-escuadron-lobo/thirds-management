spring:
  application:
    name: thirds

  profiles:
    active:
    - ${PROFILE:dev}

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql:
              BasicBinder: TRACE